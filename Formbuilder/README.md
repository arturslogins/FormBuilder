# TEST

# FormBuilder
Composer package for dynamic form building in Laravel 5.2 framework

## Installation

Create new folder 'packages

-RUN `composer require arthur/formbuilder`


-ADD `Formbuilder\FormServiceProvider::class,` to **providers** in *config/app.php*
-Run `php artisan vendor:publish`

## Usage
